{
  "name": "cubby-bookshelf-3d",
  "version": "1.0.0",
  "description": "Interactive 3D cubby bookshelf viewer built with Three.js",
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site — no build step needed'"
  }
}
